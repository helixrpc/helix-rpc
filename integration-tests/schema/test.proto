syntax = "proto3";
package helix.example;

message UserProfile {
	int64 user_id = 1;
	string username = 2;
	string email = 3;
}

service UserProfileService {
	rpc GetUserProfile (UserProfile) returns (UserProfile);
}
