syntax = "proto3";
package helix.transcoding;

message UserProfile {
	int64 user_id = 1;
	string username = 2;
	string email = 3;
}

service TranscodingService {
	rpc GetProfile (UserProfile) returns (UserProfile) {
		option (google.api.http) = {
			get: "/v1/profile/{user_id}"
		};
	}
}
